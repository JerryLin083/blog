import loading from "../assets/icons-loading.svg";

function Loading() {
  return (
    <div class="loading">
      <img src={loading} alt="loading" height="64" width="64" />
    </div>
  );
}

export default Loading;
